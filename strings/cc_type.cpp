#include <iostream>
#include <cctype>
using namespace std;
int main(int argc, char const *argv[])
{
    char a = 'v';
    bool b = isalpha(a);
    // isalpha, isalnum, isdigit, islower, isupper, isspace, isprint, ispunct

    cout<<(char) toupper(a);


}
